from read_json_file import read_json_file


#[rating, length of title, length of text, user_ID, timestamp, helpful_vote, product_ID, verified_purchase] * 16216 (buy record, not user)


def label(matrix):
    return 